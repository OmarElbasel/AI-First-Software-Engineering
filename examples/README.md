# Examples

Planned: the full **Invoicely** reference implementation — the SaaS that every
handbook chapter builds pieces of, assembled into one runnable system.

- FastAPI backend (Stage 3), Next.js frontend (Stage 4), React Native app
  (Stage 5), PostgreSQL schema and migrations (Stage 6), Docker + CI/CD
  (Stage 7), test suite (Stage 8), hardening (Stage 9), scaling setup
  (Stage 11), billing and analytics (Stage 12)
- Every non-obvious decision links back to the chapter that explains it

This is a **reference to read**, optimized for learning. When you want a
template to start your own product from, use [`starter-kits/`](../starter-kits/)
— it is extracted from this codebase once it exists.

Status: not started. The prose curriculum (stages 1–12) came first; this is
the next major milestone.
